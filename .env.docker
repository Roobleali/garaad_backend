DEBUG=False
SECRET_KEY=change-this-to-a-secure-key-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database settings - these will be overridden by the DATABASE_URL in docker-compose.yml
DATABASE_URL=postgres://postgres:postgres@db:5432/garaad_db

# The following are not needed when using DATABASE_URL but included for reference
DB_NAME=garaad_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432 